// Firebase/FCM is not configured. Push notifications are disabled.
// Will be replaced with fast2sms or similar when ready.
const initializeFirebase = () => null;
const getMessaging = () => null;

module.exports = { initializeFirebase, getMessaging };
